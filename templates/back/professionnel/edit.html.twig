{% extends 'base.html.twig' %}

{% block content %}
<div class="container mt-5">
    <h1>{{ professionnel.id ? 'Modifier le professionnel' : 'Ajouter un nouveau professionnel' }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <div class="text-center">
            <button type="submit" class="btn btn-primary">{{ professionnel.id ? 'Modifier' : 'Ajouter' }}</button>
        </div>
    {{ form_end(form) }}
</div>
{% endblock %}
